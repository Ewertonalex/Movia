[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# A Netlify injeta a versão atual do adaptador OpenNext.
# Não pinamos @netlify/plugin-nextjs no package.json.
[[plugins]]
  package = "@netlify/plugin-nextjs"
